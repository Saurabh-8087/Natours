# Ignore node_modules folder
node_modules/

# Ignore config.env (to prevent sensitive data being uploaded)
config.env

# Ignore package-lock.json (optional, if you want to avoid it as well)
package-lock.json

# Ignore node_modules folder
.cache/